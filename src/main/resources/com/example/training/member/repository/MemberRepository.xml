<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.training.member.repository.MemberRepository">
	<select id="findByEmail"
		resultType="com.example.training.member.domain.Member">
		SELECT
		*
		FROM
		members
		WHERE
		email = "${email}"
	</select>

	<select id="findById"
		resultType="com.example.training.member.domain.Member">
		SELECT * FROM members WHERE id = "${id}"
	</select>

	<select id="findAll"
		resultType="com.example.training.member.domain.Member">
		SELECT * FROM members
	</select>


	<select id="findAddress"
		resultType="com.example.training.member.domain.Member">
		SELECT
		last_name,
		first_name,
		email,
		postcode,
		prefecture,
		city,
		block,
		phone_number
		FROM
		members
		m
		WHERE m.email = "${email}"
	</select>

	<insert id="create" useGeneratedKeys="true">
		INSERT INTO members (email, password, last_updated_by, status) VALUES (
			"${memberApplicationForm.email}",
			"${digest}",
			"none",
			"unapproved"
		)
	</insert>

	<select id="countByEmail" resultType="int">
		SELECT COUNT(*) FROM members WHERE email = #{email}
	</select>

	<update id="update">
    UPDATE members SET
			email=#{memberEditForm.email},
			last_updated_by=#{lastUpdatedBy},
			status=#{memberEditForm.status}
			WHERE id= #{memberEditForm.id}
	</update>

	<update id="updateAtOrder">
		UPDATE members SET
		first_name=#{orderForm.firstName},
		last_name=#{orderForm.lastName},
		phone_number=#{orderForm.phoneNumber},
		postcode=#{orderForm.postcode},
		prefecture=#{orderForm.prefecture},
		city=#{orderForm.city},
		block=#{orderForm.block}
		WHERE id= #{orderForm.memberId}
	</update>

</mapper>
